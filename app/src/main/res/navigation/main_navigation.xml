<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_navigation"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.finki.mpip.memorize.HomeFragment"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home" />
    <fragment
        android:id="@+id/stationeryNearbyFragment"
        android:name="com.finki.mpip.memorize.StationeryNearbyFragment"
        android:label="fragment_stationery_nearby"
        tools:layout="@layout/fragment_stationery_nearby" />
    <fragment
        android:id="@+id/decksFragment"
        android:name="com.finki.mpip.memorize.DecksFragment"
        android:label="fragment_decks"
        tools:layout="@layout/fragment_decks" />
    <fragment
        android:id="@+id/profileFragment"
        android:name="com.finki.mpip.memorize.ProfileFragment"
        android:label="fragment_profile"
        tools:layout="@layout/fragment_profile" />
</navigation>